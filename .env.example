# Values consumed by docker-compose.yml. Copy to `.env` at the repository root.
# Per-service configuration lives in backend/.env and frontend/.env.local.

POSTGRES_USER=dataguardian
POSTGRES_PASSWORD=dataguardian
POSTGRES_DB=dataguardian
